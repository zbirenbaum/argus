  docker exec -it argus-arm64 /build/target/aarch64-unknown-linux-musl/debug/supervisor \
    --agent-id demo \
    -- claude

docker run --rm --name argus-demo \
  --cap-add SYS_PTRACE \
  --security-opt seccomp=unconfined \
  --security-opt apparmor=unconfined \
  -e CLAUDE_CODE_OAUTH_TOKEN="$DEV_CLAUDE_CODE_OAUTH_TOKEN" \
  -e HOME=/home/agent \
  -p 9090:9090 \
  -p 8000:8000 \
  tmux new-session -d -s claude \
  /build/target/aarch64-unknown-linux-musl/debug/supervisor \
  --agent-id demo --config /tmp/config.yaml \
  -- claude
