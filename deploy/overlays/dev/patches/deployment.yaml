apiVersion: apps/v1
kind: Deployment
metadata:
  name: argus-agent
spec:
  template:
    spec:
      containers:
        - name: agent
          env:
            - name: RUST_LOG
              value: "debug"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 512Mi
